name: Resume RAG Chatbot CI/CD

on:

  push:
    branches:
      - main


jobs:

<<<<<<< HEAD
  build-and-test:
=======
  build-and-push:
>>>>>>> b7682d2 (Added Docker Hub CI/CD pipeline)

    runs-on: ubuntu-latest


    steps:

      # ==============================
<<<<<<< HEAD
      # Checkout Repo
=======
      # Checkout Repository
>>>>>>> b7682d2 (Added Docker Hub CI/CD pipeline)
      # ==============================

      - name: Checkout Repository
        uses: actions/checkout@v4


      # ==============================
<<<<<<< HEAD
      # Python Setup
      # ==============================

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"


      # ==============================
      # Install Backend Dependencies
      # ==============================

      - name: Install Backend Dependencies
        run: |
          cd backend
          pip install -r requirements.txt


      # ==============================
      # Node Setup
      # ==============================

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"


      # ==============================
      # Install Frontend Dependencies
      # ==============================

      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm install


      # ==============================
      # Docker Build Test
      # ==============================

      - name: Build Docker Containers
        run: |
          docker compose build
=======
      # Docker Login
      # ==============================

      - name: Login to Docker Hub
        uses: docker/login-action@v3

        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      # ==============================
      # Build Backend Image
      # ==============================

      - name: Build Backend Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/rag-backend ./backend


      # ==============================
      # Push Backend Image
      # ==============================

      - name: Push Backend Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/rag-backend


      # ==============================
      # Build Frontend Image
      # ==============================

      - name: Build Frontend Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/rag-frontend ./frontend


      # ==============================
      # Push Frontend Image
      # ==============================

      - name: Push Frontend Docker Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/rag-frontend
>>>>>>> b7682d2 (Added Docker Hub CI/CD pipeline)
